# ebank 月度資料自動更新腳本 — 使用說明

## 概覽

每個月只需 **3 個步驟**：

```
登入 ebank → 開啟 DevTools → 按 Ctrl+Enter
```

腳本會自動比對上次執行的記錄，**只下載新月份的資料**，不會重複抓舊資料。

---

## 第一次設定（只需做一次）

### 步驟 1：開啟 Chrome DevTools Snippets

1. 用 Chrome 登入 `https://ebank.banking.gov.tw/Frame`
2. 按 **F12** 開啟開發者工具
3. 點選上方 **Sources** 頁籤
4. 左側找到 **Snippets**（可能要展開 `>>` 選單才看得到）

   ![示意](https://i.imgur.com/placeholder.png)

5. 點 **New snippet**，命名為 `ebank_updater`

### 步驟 2：貼上腳本

1. 開啟 `ebank_monthly_updater.js`（用任何文字編輯器）
2. 全選（Cmd+A）→ 複製（Cmd+C）
3. 在 Snippets 編輯區全選 → 貼上（Cmd+V）
4. 按 **Ctrl+S** 儲存

> ✅ 設定完成！之後每個月只需執行步驟 3。

---

## 每月執行流程

### 步驟 3：執行腳本

1. 登入 `https://ebank.banking.gov.tw/Frame`
2. 按 F12 → Sources → Snippets
3. 點選 `ebank_updater`
4. 按 **Ctrl+Enter**（或右鍵 → Run）

畫面右上角會出現**進度面板**，顯示：

```
📊 ebank 月度資料更新
上次紀錄：民國 115 年 02 月，搜尋新資料...
[==========          ] 50%
項目 14 / 27   新增筆數 38   略過 12   錯誤 0
✓ EC002W：新增 5 筆
✓ EC011W：無新資料
...
✅ 完成！新增 76 筆 → ebank_update_115_03.json
```

5. 完成後，Chrome **下載資料夾**會出現：

   ```
   ebank_update_115_03.json   ← 本月新增資料
   ```

---

## 輸出檔案格式

```json
{
  "extractedAt": "2026-03-27T10:30:00.000Z",
  "latestYrMn": { "yr": 115, "mn": 3 },
  "totalNewRecords": 76,
  "data": {
    "EC002W_儲值卡發行資料維護": {
      "totalRows": 150,
      "newRows": 5,
      "details": [
        {
          "yr": 115, "mn": 3, "sn": 1,
          "機構名稱": "一卡通票證股份有限公司",
          "發卡總數": "56500000",
          ...
        }
      ]
    },
    ...
  }
}
```

---

## 進階操作

### 🔄 重設（重新全部抓取）

如果要重新抓所有歷史資料（例如資料遺失、換電腦），在 Chrome Console 執行：

```javascript
localStorage.removeItem('ebank_last_update')
```

然後重新執行腳本，會從最近 3 個月開始抓。

### 📅 調整「首次執行往前抓幾個月」

修改腳本頂端 CONFIG 區：

```javascript
const CONFIG = {
  FIRST_RUN_MONTHS: 3,   // ← 改成 6、12 等
  ...
};
```

### 🗓️ 查看上次執行記錄

在 Console 執行：

```javascript
JSON.parse(localStorage.getItem('ebank_last_update'))
// 輸出：{ yr: 115, mn: 2 }
```

### ⚡ 換電腦後的設定

1. 把 `ebank_monthly_updater.js` 複製到新電腦
2. 重新照「第一次設定」操作
3. 在新電腦 Console 手動設定上次已抓到的年月，避免重複下載：
   ```javascript
   localStorage.setItem('ebank_last_update', JSON.stringify({yr: 115, mn: 2}))
   ```

---

## 歷史資料管理建議

每月執行後，可用以下 Python 腳本將增量檔案合併到主資料庫（選用）：

```python
import json, glob, os

# 載入主資料庫（如果存在）
master_file = 'ebank_master.json'
master = json.load(open(master_file)) if os.path.exists(master_file) else {}

# 載入所有增量更新檔
for f in sorted(glob.glob('ebank_update_*.json')):
    update = json.load(open(f))
    for item_name, item_data in update['data'].items():
        if item_name not in master:
            master[item_name] = {'details': []}
        # 去重後合併
        existing_keys = {
            f"{r['yr']}|{r['mn']}|{r.get('sn','')}"
            for r in master[item_name]['details']
        }
        new_records = [
            r for r in item_data['details']
            if f"{r['yr']}|{r['mn']}|{r.get('sn','')}" not in existing_keys
        ]
        master[item_name]['details'].extend(new_records)
    print(f"已合併 {f}：{update['totalNewRecords']} 筆")

# 儲存主資料庫
json.dump(master, open(master_file, 'w', encoding='utf-8'), ensure_ascii=False, indent=2)
print(f"主資料庫已更新：{master_file}")
```

---

## 常見問題

| 問題 | 解法 |
|------|------|
| 面板出現但所有項目都顯示「DataTable 載入逾時」 | 確認已登入，且是在 `/Frame` 主頁執行 |
| 下載後找不到檔案 | 到 Chrome 設定 → 下載，確認下載位置 |
| 執行中畫面卡住 | 正常現象，同步抓取中，等待即可（通常 2-5 分鐘）|
| 想跳過某幾個項目 | 修改腳本 `SUB_ITEMS` 陣列，刪除不需要的項目 |
| Chrome 詢問「允許多個下載」| 點「允許」 |
